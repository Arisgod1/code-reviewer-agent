## 初始化
```bash
mkdir code-reviewer-agent
cd code-reviewer-agent
go mod init github.com/yourname/code-reviewer-agent
```
#### 安装 cobra
```bash
go get github.com/spf13/cobra@latest
```
#### 创建目录
```bash
mkdir -p cmd/cli internal/{types,tools,report} testdata
touch cmd/cli/main.go
touch internal/types/types.go
touch internal/tools/parse_diff.go
touch internal/tools/scan_rules.go
touch internal/tools/format_report.go
touch testdata/sample.patch
```
#### 定义types
### 做可运行的CLI
将main.go写了一下，主要是用cobra把这个CLI搭出来，能接受diff和lang参数，然后定义了一个tool，parse_diff的数据结构。
```bash
go run ./cmd/cli --diff testdata/sample.patch --lang go
```
### 把mock换成真实解析
对git diff的标准输出字符串进行解析，用不同的字符加以区分。
### 做一个可用规则扫描器
两个，一个是sql拼接，一个是敏感信息日志的打印，也就是打印的函数和敏感信息的字段同时出现的一个扫描。

然后记得把SacnRiskRules的函数调用写在main.go中。
### 做一个报告的fmt文件
在format_report.go文件中写完对应的代码后，在main加入对应报告输出除了json的还有一个markedown文档的。
然后加入了一个parse_diff的单元测试文件。
加了一条apikey显式写入的检查规则
### 加入trace
用来追踪运行
### 封装成tool调用
把tools封装成agent可以调用的形式而不关心其内部实现。

然后**recordToolCall**使用了一个AOP风格的Go语言实现，用**高阶函数**实现非侵入性的性能监控和日志记录。
```go
recordToolCall := func(name string, fn func() (map[string]any, error)) (map[string]any, error) {  
    start := time.Now()  
    out, err := fn()  
    cost := time.Since(start).Milliseconds()  
  
    tc := types.ToolCall{  
       Name:    name,  
       CostMs:  cost,  
       Success: err == nil,  
    }  
  
    if err != nil {  
       tc.Output = "error: " + err.Error()  
    } else {  
       tc.Output = "ok"  
    }  
  
    toolCalls = append(toolCalls, tc)  
    return out, err  
}
```
调用：
```go
parseOut, err := recordToolCall("parse_diff", func() (map[string]any, error) {  
    return parseTool.Run(ctx, map[string]any{  
       "diff_path": diffPath,  
    })  
})
```
这块看着有一丝不好理解。

### context超时控制
在call那个函数里面添加了2s的超时控制，防止一个工具卡死后面不运行。

### 加入不添加llm的agent包
实现了ReAct中的loop结构，将trace等定义拿出来，解耦合。